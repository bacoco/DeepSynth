# IMPORTANT: This file includes ALL dependencies including GPU training libraries
#
# For macOS or CPU-only dataset generation, use:
#   pip install -r requirements-base.txt
#
# For Linux with CUDA for training, use:
#   pip install -r requirements-base.txt
#   pip install -r requirements-training.txt
#
# Or use this file directly (may fail on macOS due to xformers):
#   pip install -r requirements.txt

torch==2.8.0
torchvision==0.23.0
transformers>=4.46.0
tokenizers>=0.20.0
datasets>=2.14.0
huggingface_hub>=0.20.3
accelerate>=0.24.0
xformers>=0.0.22  # GPU-only, will fail on macOS
peft>=0.11.1  # LoRA/QLoRA fine-tuning
bitsandbytes==0.43.1  # 4-bit quantization for QLoRA (pinned for CUDA compatibility)
rouge-score>=0.1.2
bert-score>=0.3.13
nltk>=3.8
pillow>=9.5.0
tqdm>=4.65.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0
flask>=3.0.0
werkzeug>=3.0.0
pytesseract>=0.3.10
python-dotenv>=1.0.0
